import { BaseCommand, BaseCommand } from '@discord-factory/core'
import { Message } from 'discord.js'

@BaseCommand({ label: '$className command', description: '$className description', tag: '$tag' })
export default class $className implements BaseCommand {
	public async run(message: Message, args: Array<string>): Promise<void> {
		// Your code here
	}
}
