import { Command, BaseCommand } from '@discord-factory/core'
import { Message } from 'discord.js'

@Command({ label: '$className command', description: '$className description', tag: '$tag' })
export default class $className implements BaseCommand {
	public async run(message: Message, args: Array<string>): Promise<void> {
		// Your code here
	}
}
